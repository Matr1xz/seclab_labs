# results.config
# 
# Please see the Labtainer Lab Designer User Guide
#
# httpd log entries reflecting access to the business plan, from local and then remote
gzip = *.stdout : CONTAINS : Content-Encoding: gzip
cache = *.stdout : CONTAINS : HIT
time = /etc/nginx/nginx.conf : CONTAINS : client_header_timeout
